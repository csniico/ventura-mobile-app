import 'package:flutter/material.dart';

class CreateCustomers extends StatelessWidget {
  const CreateCustomers({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Create Customers Page', style: TextStyle(fontSize: 24)),
    );
  }
}
