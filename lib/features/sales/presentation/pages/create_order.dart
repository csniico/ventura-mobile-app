import 'package:flutter/material.dart';

class CreateOrder extends StatelessWidget {
  const CreateOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Create Order Page', style: TextStyle(fontSize: 24)),
    );
  }
}
