import 'package:flutter/material.dart';

class Marketing extends StatelessWidget {
  const Marketing({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Marketing Screen'),
    );
  }
}
