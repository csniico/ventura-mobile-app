enum AppBarType {
  /// Dashboard app bar with user profile, avatar, and search
  dashboard,

  /// Secondary pages with back button and title
  secondary,

  /// Minimal app bar with just title
  minimal,
}
